Using Caffe Container
Note: Caffe is Built without OpenCV support.

Run command:
$docker run .-it --name=caffe_container  caffe


Now you can verify caffe by typing :
$caffe --version


Get help with following command:
$caffe --help

