# ###################################
# This file is generated by puppet
# PLEASE DON'T MODIFY BY HAND
# ###################################


  ########################
  # CHE
  ########################
  che:
    image: eclipse/che-server:nightly
    mem_limit: 
    memswap_limit: 0
    env_file:
      - '/root/lysanne/che/dockerfiles/cli/tests/testrun/cli_cmd_start_with_custom_port/instance/config/che.env'
    volumes:
      - '/var/run/docker.sock:/var/run/docker.sock'
      - '/data:/data'
      - '/logs:/logs'
      - '/config/che:/conf'
      - '/etc/group:/etc/group:ro'
      - '/etc/passwd:/etc/passwd:ro'
    ports:
      - ':'
    restart: always
    container_name: chetest
    user: 

